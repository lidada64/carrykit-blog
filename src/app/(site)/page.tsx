import { PagePlaceholder } from "@/components/ui/page-placeholder";

/** Home 占位(M1-7 实现 hero + 精选作品 + 最新博客) */
export default function HomePage() {
  return <PagePlaceholder titleKey="common.home" />;
}
